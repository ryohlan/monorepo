{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "mobile",
      "core",
      "web"
    ]
  },
  "scripts": {
    "clean": "rm -rf node_modules **/node_modules **/yarn.lock **/package-lock.json yarn.lock yarn-error.log",
    "clean-install": "yarn clean && yarn install"
  },
  "dependencies": {},
  "devDependencies": {}
}
