{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "mobile",
      "core",
      "web"
    ]
  },
  "scripts": {
    "clean": "rm -rf node_modules　yarn.lock **/node_modules **/yarn.lock **/package-lock.json  yarn-error.log",
    "clean-install": "yarn clean && yarn install"
  },
  "dependencies": {},
  "devDependencies": {}
}
